/**
 * Classe que representa os canhões das naves
 */

class Cannon {

    /**
     * Inicia com o design do canhão
     * @param {THREE.mesh} mesh 
     */
    constructor (mesh) {
        this.mesh = mesh;        
    }

    /**
     * Muda a cor do canhão
     * @param {hexadecimal number} color 
     */
    setColor(color) {
        
    }
}